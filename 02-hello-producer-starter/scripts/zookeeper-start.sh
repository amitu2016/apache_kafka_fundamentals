#!/bin/bash
# Confluent 8.0.0 uses KRaft mode, no Zookeeper needed
# This script is kept for reference but not needed in KRaft mode
echo "Confluent 8.0.0 uses KRaft mode - no Zookeeper needed"
echo "Use the kafka-server-start.sh scripts directly"
